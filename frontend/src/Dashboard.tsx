import { Text } from "@chakra-ui/react"
import {useEffect} from "react";

export default function Dashboard() {

    useEffect(() => {

    } ,[])

    return (
        <div>
            <Text>Dashboard</Text>
        </div>
    )
}